#!/usr/bin/env bash
# Script that tells the story of /etc/passwd

while IFS=":" read -r username password userid groupid comment home shell
do
    echo "The user $username ($comment) is part of the $groupid gang, lives in $home and rides $shell. USER ID's place is protected by the passcode $password, more info about the user here: $userid"
done < /etc/passwd
